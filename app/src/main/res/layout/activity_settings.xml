<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/layout_margin"
    android:orientation="vertical">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/lenght_selector"
        android:text="@string/spinner_view_header"
        android:textSize="18dp" />

    <Spinner
        android:id="@+id/lenght_selector"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginTop="@dimen/view_margin"></Spinner>

    <Button
        android:id="@+id/generatehex"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/lenght_selector"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="@dimen/view_margin"
        android:text="@string/button_generate_text" />

</RelativeLayout>